# Benchmarks

No benchmarks have been run.

## Required Report Metadata

Future benchmark reports must record commit, Dart version, Python version,
reference library version, compiler, native backend, OS, CPU, GPU, RAM, power
mode, dtype, shape, warm-up count, measured iterations, and synchronization
method.

## Current Status

Phase 0 has no performance-sensitive implementation yet. Any benchmark,
performance, or parity claim is `NOT VERIFIED`.
